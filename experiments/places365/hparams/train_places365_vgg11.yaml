################################################
#                                              #
#  Model:                                      #
#  Author: vbadenas (victor.badenas@gmail.com) #
#                                              #
################################################

# seeds
seed: 42
__set_seed: !apply:torch.manual_seed [!ref <seed>]
experiment_name: "places365_vgg11"
experiment_folder: "results/places365_vgg11/"
device: "cuda:0"

# data folder
data_folder: ./data/

# training parameters
epochs: 50
batch_size: 1
shuffle: True

num_clases: 365

transform_tensor: !new:torchvision.transforms.ToTensor

preprocessing: !new:torchvision.transforms.Compose
    transforms: [
        !ref <transform_tensor>,
    ]

# dataset object
train_dataset: !new:torchvision.datasets.Places365
    root: !ref <data_folder>
    split: 'train-standard'
    small: true
    download: false
    transform: !ref <preprocessing>

valid_dataset: !new:torchvision.datasets.Places365
    root: !ref <data_folder>
    split: 'val'
    small: true
    download: false
    transform: !ref <preprocessing>

# dataloader options
dataloader_options:
    batch_size: !ref <batch_size>
    shuffle: !ref <shuffle>
    num_workers: 0

opt_class: !name:torch.optim.Adam
    lr: 0.001

loss: !new:torch.nn.CrossEntropyLoss

metrics: !new:frarch.modules.metrics.MetricsWrapper
    classification_error: !new:frarch.modules.metrics.ClassificationError

model: !apply:frarch.models.classification.cnn.vgg11
    pretrained: false

classifier: !apply:frarch.models.classification.cnn.vggclassifier
    pretrained: false

modules:
    model: !ref <model>
    classifier: !ref <classifier>

checkpointer: !new:frarch.modules.Checkpointer
    save_path: !ref <experiment_folder>
    reference_metric: "classification_error"
    modules:
        model: !ref <model>
        classifier: !ref <classifier>
